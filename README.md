# URL to PDF

A node.js script that builds to a binary that takes a URL to a PDF and downloads it to the current directory.

To build the binary, run `npm run build`. The binary will be in the root directory and named `url-to-pdf`.
